import styled from "styled-components";

export const Container = styled.button`
    width: 100%;
    height: 3rem;
    border: 0;
    border-radius: 8px;
    font-size: 1.25rem;
    font-weight: 700;
    margin-top: 2rem;
    color: var(--white);
    background: var(--primary);
`